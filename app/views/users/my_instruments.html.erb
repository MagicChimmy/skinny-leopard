<h1 class="header-style">Your Listings:</h1>

<div class="container cards-container">
  <div class="row">
    <% @instruments.each do |i| %>
    <div class="col-sm-4">
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path i.photo, height: 300, width: 400, crop: :fill unless i.photo.blank? %>')">
        <h3><%= link_to i.name.capitalize, instrument_path(i), class: "hyperlink" %></h3>
        <p>£<%= i.price %> /day</p>
          <div class="container">
            <div class="card-button-top-right"><%= link_to instrument_path(i), id: "icon-button", method: :delete, data: { confirm: "Are you sure?" } do %>
              <i class="fas fa-dumpster"></i>
            <% end %>
            </div>
            <div class="card-button-btm-left"><%= link_to "Edit this listing", edit_instrument_path(i), class: "btn btn-primary", id: "button" %>
            </div>
          </div>
      </div>
    </div>
    <% end %>
  </div>
</div>
